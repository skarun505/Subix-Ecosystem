{
    "name": "@subix/leados",
    "version": "1.0.0",
    "private": true,
    "description": "leados.subix.in — Lead Management System",
    "scripts": {
        "dev": "serve app -l 3002",
        "build": "echo 'Static site — no build needed'",
        "clean": "echo 'Nothing to clean'"
    },
    "devDependencies": {
        "serve": "^14.2.0"
    },
    "dependencies": {
        "@subix/config": "workspace:*",
        "@subix/auth": "workspace:*",
        "@subix/db": "workspace:*",
        "@subix/ui": "workspace:*"
    }
}