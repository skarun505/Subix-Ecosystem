{
    "name": "@subix/web",
    "version": "1.0.0",
    "private": true,
    "description": "subix.in — Marketing & landing site",
    "scripts": {
        "dev": "serve . -l 3000",
        "build": "echo 'Static site — no build needed'",
        "clean": "echo 'Nothing to clean'"
    },
    "devDependencies": {
        "serve": "^14.2.0"
    },
    "dependencies": {
        "@subix/config": "workspace:*",
        "@subix/ui": "workspace:*"
    }
}